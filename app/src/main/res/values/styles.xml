<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/himalaya_color</item>
        <item name="colorPrimaryDark">@color/himalaya_color</item>
        <item name="colorAccent">@color/himalaya_color</item>
    </style>

</resources>
